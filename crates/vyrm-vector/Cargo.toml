[package]
name = "vyrm-vector"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Exact vector oracle, immutable projections, HNSW, and search planner for Vyrm."

[features]
default = []
accelerator = []

[dependencies]
vyrm-core = { path = "../vyrm-core" }
serde = { workspace = true }
serde_json = { workspace = true }
memmap2 = "0.9"

[dev-dependencies]
tempfile = "3"
vyrm-store = { path = "../vyrm-store" }
